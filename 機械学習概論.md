# 機械学習概論：期末テスト・レポート対策まとめノート

## 1. LASSOと正則化（モデルのお掃除屋さん）
* **LASSO回帰**: 不要な変数の重み（係数）を強制的に「0」にして、モデルをシンプルにする手法。
* **スパース（疎）な解**: 係数のほとんどが「0」で、一部の重要な変数だけが残っている状態のこと。不要な変数が消えるため、モデルの「解釈性」が高くなり、「過学習」を防ぐことができる。
* **L1正則化**: 「||w||_1 <= s」のように、重みの絶対値の和を予算(s)以下に抑えるペナルティのこと。グラフにすると制約領域が「ひし形」になり、予測精度の等高線がひし形の「角」にぶつかりやすいため、スパース解（一部の変数が0になる）が得られやすい。

## 2. 誤差（Error）の3兄弟と過学習
* **訓練誤差 (Training Error)**: 手元の学習データ（練習問題）でのミス。モデルを複雑にするほど下がるが、下げすぎると「丸暗記（過学習）」になる。
* **検証誤差 (Validation Error)**: ハイパーパラメータ（予算の厳しさであるλなど）を決定するための「模試」でのミス。この誤差が最小になる設定を選ぶ。
* **汎化誤差 (Generalization Error)**: 未知のデータ（本番）に対する本当の実力。**機械学習の最終目的は、この汎化誤差を最小にすること。**

## 3. 汎化誤差を正しく測る（評価する）手法
モデルが過学習していないか、未知のデータへの対応力を測るための「データの分け方」。

* **ホールドアウト法**: データを「学習用」と「テスト用」の2つにバッサリ分ける手法。シンプルだが、分け方による運要素がある。
* **K-fold（K-分割）交差検証**: データをK個に分け、テスト役を交代しながらK回学習と評価を繰り返し、平均をとる手法。データを無駄なく使え、偏り（ばらつき）を抑えられる。
* **ブートストラップ法**: データを「重複あり」で引き直して（リサンプリング）、擬似的に複数のデータセットを作る手法。
    * **0.632の法則**: ブートストラップで抽出を繰り返すと、元のデータの約63.2%が選ばれ、約36.8%が一度も選ばれずに余る。この余ったデータ（OOBデータ: Out-of-Bag）を、天然のテストデータとして評価に活用できる。

## 4. 機械学習の必勝フロー（全体像）
1. **データの分割**: ホールドアウト法やK-fold法を用いて、最終テスト用のデータを確保（隠蔽）する。
2. **学習と正則化**: 訓練データを使ってAIに学習させる。この時、LASSO（L1正則化）などでペナルティを与え、丸暗記（過学習）を防止する。
3. **ハイパーパラメータの選択**: 検証誤差を指標にして、モデルの複雑さ（λなどのルールの厳しさ）を最適に調整する。
4. **汎化誤差の評価**: 最後に、隠しておいたテストデータ（またはOOBデータ）を用いて汎化誤差を測り、モデルの真の実力を評価する。

# 機械学習概論：決定木とアンサンブル学習のまとめ

## 1. 決定木 (Decision Tree)
「もし〜なら」の条件分岐を繰り返し、ツリー状にデータを分類・予測する手法。

* **主要な用語:**
  * **不純度 (Impurity):** データの混ざり具合（ジニ係数など）。AIはこれを下げるように木を成長させる。
  * **情報利得:** 分割によってどれだけ不純度が下がったか（スッキリしたか）。
  * **特徴量重要度:** どの変数が予測に最も貢献したかを示す指標。
* **強み:** 理由が説明しやすい（ホワイトボックス）、前処理が楽ちん。
* **課題:** 深くしすぎると特定のデータに特化しすぎる「過学習」を起こす。データのわずかな変化に弱い。

---

## 2. アンサンブル学習 (Ensemble Learning)
複数のモデル（弱学習器）を組み合わせて、単体よりも精度の高い強力なモデルを作る手法。

* **バギング (Bagging):** 並列に複数のモデルを作り、多数決をとる。（代表例：ランダムフォレスト）
* **ブースティング (Boosting):** 直列にモデルを作り、前のモデルのミスを次のモデルが修正していく。（代表例：AdaBoost）

---

## 3. ランダムフォレスト (Random Forest)
決定木の弱点（過学習）を克服するための手法（決定木 × バギング）。

* **仕組み:** 「データの抽出」と「使う特徴量（質問項目）」を毎回ランダムにして多様な決定木を大量に作成し、全員で多数決をとる。
* **強み:** 過学習に非常に強い、高精度、特徴量重要度がわかる。
* **課題:** なぜその答えになったのか説明しにくくなる（ブラックボックス化）、大量の木を作るので計算・メモリが重い。

---

## 4. AdaBoost (Adaptive Boosting)
苦手な問題を克服しながらリレー形式で学習する手法（決定木 × ブースティング）。

* **仕組み:** 深さ1の超単純な木（スタンプ）を順番に作る。前の木が間違えたデータに「重み」をつけ、次の木は「重み付き不純度」を使ってそのミスを重点的に学習する（損失関数の逐次最小化）。
* **強み:** 単純なモデルの組み合わせで高い精度が出せる、難しい境界線の分類が得意。
* **課題:** 間違ったデータ（ノイズ・外れ値）に過剰反応して変なルールを作ってしまう、直列処理のため並列計算ができず時間がかかる。

---

### 比較表：ランダムフォレスト vs AdaBoost
| 比較項目 | ランダムフォレスト (Bagging) | AdaBoost (Boosting) |
| :--- | :--- | :--- |
| **学習の進め方** | 並列（みんなで一斉に作る） | 直列（前のミスを受けて順番に作る） |
| **木の深さ** | 比較的深い（専門家） | 深さ1のスタンプ（素人） |
| **データの扱い** | 毎回ランダムに選ぶ | 間違えたデータの「重み」を増やす |
| **最終決定** | 平等な多数決 | 優秀なモデルの意見を重視する多数決 |
| **最大の弱点** | 計算コスト、ブラックボックス化 | 外れ値（ノイズ）に非常に弱い |